#include "takuzu.h"
#include "util.h"
#include "io.h"
#include "game.h"

#define SIZE 4


int main(int argc, char * argv[]) {

    printBanner();
    mainMenu();

/*    PROPOSITION proposotion1 = {{1, 1}, 1};
    PROPOSITION proposotion2 = {{1, 2}, 1};
    LL list = initListCoup(proposotion1);
    appendCoup(proposotion1, list);
    appendCoup(proposotion2, list);

    printCoups(list);*/

//    solveGrid(createGame(SIZE));


    return 0;
}